DTD is not a good solution for validating xml. 
-DTD will not work If you have an ORM schema that absorbs the same object more than once
-DTD will not work if you have an ORM schema that has a ring facttype.